# Superpowers tooling state (brainstorm sessions, local servers, etc.)
.superpowers/

# macOS Finder metadata
.DS_Store

# Feedback triage workflow — local admin credentials + fetched data (never commit)
supabase/.env.local
docs/feedback/inbox.json
